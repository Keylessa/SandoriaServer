#pragma once

class SWorldManager
{
public:
    SWorldManager();
    ~SWorldManager();

    void Initialize();    // Inițializare lume
    void Update();        // Actualizare lume

private:
    // Adaugă variabile pentru gestionarea instanțelor și a zonei de joc
};
